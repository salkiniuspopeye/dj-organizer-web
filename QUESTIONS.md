# Offene Fragen an Product Owner

1.  **"Empty/Errors freundlich" (P0 Task):** Die aktuelle Implementierung verwendet für den Directory-Picker-Guard eine spezifische, freundliche Fehlermeldung. Andere Fehler (z.B. von `handleGenerateAndExecuteMovePlan`) verwenden noch generische `alert()`-Meldungen. Sollen alle Fehler mit einer benutzerdefinierten UI behandelt werden, oder sind `alert()`-Meldungen für weniger häufige Fehler akzeptabel?
2.  **"Preview starts at 35% of track duration" vs. "Play/Pause ab 0:00":** Der `PROJECT_BRIEF.md` besagt "Preview starts at 35% of track duration", aber die Sprint 1 P0-Aufgabe war "Play/Pause ab 0:00". Ich habe "Play/Pause ab 0:00" implementiert. Welches ist das gewünschte Verhalten für die Audio-Vorschau?
3.  **"Fallback „Dateien wählen“ (input multiple webkitdirectory)":** Das `webkitdirectory`-Attribut ist nicht standardisiert. Ist es akzeptabel, sich für den Fallback darauf zu verlassen, oder sollte eine standardmäßigere Dateiauswahl (Datei für Datei) in Betracht gezogen werden, falls `webkitdirectory` nicht universell unterstützt wird?
4.  **"Rekursiv-Scan mit Concurrency-Limit + Skip-Liste":** Der `totalCount` in `walkDirectory` ist eine Schätzung basierend auf der Anzahl der im ersten Durchlauf gefundenen Einträge. Er berücksichtigt keine Dateien, die durch `AUDIO_EXTENSIONS` herausgefiltert werden. Soll `totalCount` nur die gefundenen *Audio-Dateien* widerspiegeln oder alle gescannten Einträge?
5.  **"Bug: Fix Vitest module resolution for test files (BLOCKED)":** Dies ist ein blockierendes Problem für zuverlässiges Testen. Was ist der Plan, um diese manuelle Konfigurationsänderung in `vite.config.ts` zu beheben?
